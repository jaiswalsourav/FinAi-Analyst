package com.finai.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataInitializer {
    // Removed runtime database seeding. Use data.sql for initial admin insertion.
}
